
let length =
    fn x ->
        if x == [] then
            0
        else
            1 + length (cdr x)

fun length x =
    if x == [] then
        0
    else
        1 + length (cdr x)

length [1,2,3]

